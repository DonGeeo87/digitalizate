name: Deploy

on:
  push:
    branches: [master]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Create .env from .env.ci
        run: |
          if [ -f .env.ci ]; then
            cp .env.ci .env
            echo "✅ .env created from .env.ci"
          else
            echo "⚠️  No .env.ci found, skipping .env creation"
          fi
          if [ -f backend/.env.ci ]; then
            cp backend/.env.ci backend/.env
            echo "✅ backend/.env created from backend/.env.ci"
          else
            echo "⚠️  No backend/.env.ci found, skipping backend .env creation"
          fi

      - name: Copy all files to VPS
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          source: "."
          target: "/opt/${{ github.event.repository.name }}"
          strip_components: 0

      - name: Deploy on VPS
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            cd /opt/${{ github.event.repository.name }}
            docker compose up -d --build
            echo "✅ ${{ github.event.repository.name }} deployed"
